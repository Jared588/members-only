<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/tailwind.css' />
  </head>
  <body class="bg-slate-900 p-0">
    <header class="p-10 flex justify-between m-0">
      <h1 class="text-3xl flex self-center text-center text-slate-300"><%= title %></h1>
      <% if (!currentUser) { %>
        <a href="/login" class="text-slate-300 text-xl self-center">Log In</a>
      <% } else { %>
        <p class="text-xl text-slate-400">Welcome, <%= currentUser.username %></p>
      <% } %>
    </header>
    <div>
      <p class="text-center">Chat goes here</p>
    </div>
  </body>
</html>
